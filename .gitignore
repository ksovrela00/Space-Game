# Скачанный инструмент, а не часть проекта: composer ставится на машину
# один раз и в репозитории ему делать нечего. А вот server/vendor лежит
# в репозитории намеренно — см. README, «Первая зависимость в проекте».
server/composer.phar

# Настройки конкретной машины (доступ к базе). У каждой свои.
server/config.local.php
